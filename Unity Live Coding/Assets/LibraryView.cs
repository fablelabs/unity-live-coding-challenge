using System.Collections.Generic;
using Books;
using UnityEngine;

public class LibraryView : MonoBehaviour
{
    [SerializeField] private List<BookDataModel> books;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
